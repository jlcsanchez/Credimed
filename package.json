{
  "name": "credimed-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end smoke + functional tests for credimed.us. The site itself is plain HTML/JS served from GitHub Pages — this package.json only exists so we can install Playwright as a devDependency.",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.js",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
